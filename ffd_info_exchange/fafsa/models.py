#from django.db import models
#from django.utils import timezone


#class Application(models.Model):
#    name_first = models.CharField(max_length=250, help_text='Your first name')
#    name_last = models.CharField(max_length=250, help_text='Your last name')
#    date_of_birth = models.DateTimeField(help_text='Your date of birth (MM/DD/YYYY)')
#    created = models.DateTimeField(default=timezone.now)
#    updated = models.DateTimeField(default=timezone.now)
